<!DOCTYPE html>

<html>
    <head>
        <title>Help</title>
        <link rel='icon' href='/img/icon.png'>
        <link rel='stylesheet' href='/css/styles.css'>
    </head>
    <body>
        <div class="main-content">
                {{>header}}
                {{! Comment: above is the partials import statement }}
                {{!notice: '>' and then partial name without extension}}
                <p>{{helptext}}</p> 
                {{!from render function}}
        </div>
         
        {{>footer}}
    </body>
</html>